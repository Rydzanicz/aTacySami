import { StowarzyszenieData } from './stowarzyszenie-data';

describe('StowarzyszenieData', () => {
  it('should create an instance', () => {
    expect(new StowarzyszenieData()).toBeTruthy();
  });
});
